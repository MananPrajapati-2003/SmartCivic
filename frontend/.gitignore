# All ignore rules are managed in the root /.gitignore
# This file is kept so tools (ESLint, editors) that scan for a local
# .gitignore still find one, but rules live at the monorepo root.

# Frontend-specific additions (if any arise) go here:
