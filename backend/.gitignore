# All ignore rules are managed in the root /.gitignore
# This file is kept so Python tooling that expects a local .gitignore
# still finds one, but rules live at the monorepo root.

# Backend-specific additions (if any arise) go here:
