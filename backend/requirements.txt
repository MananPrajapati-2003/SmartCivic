Django>=5.1,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
psycopg2-binary>=2.9
python-dotenv>=1.0
Pillow>=10.0
requests>=2.31

# ── Async task queue (Upstash Redis — serverless, no Docker) ─────────────────
celery>=5.3
redis>=5.0         # Upstash-compatible Redis client

# ── Production deployment ─────────────────────────────────────────────────────
gunicorn>=21.2          # WSGI server for Render
whitenoise>=6.6         # Serve static files without a CDN
dj-database-url>=2.1   # Parse DATABASE_URL env var (Render PostgreSQL)
